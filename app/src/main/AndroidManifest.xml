<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fibrlink.adminassistant" >

    <uses-permission android:name="android.permission.CALL_PHONE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme" >
        <activity android:name=".index.activity.LineChartViewActivity" >
        </activity>
        <activity android:name=".login.activity.LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".index.activity.LineChartActivity" >
        </activity>
        <activity android:name=".index.activity.StretchActivity" >
        </activity>
        <activity android:name=".index.activity.IdeasExpandableActivity" >
        </activity>
    </application>

</manifest>
